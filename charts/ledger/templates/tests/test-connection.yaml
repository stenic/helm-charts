apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ledger.fullname" . }}-test-connection"
  labels:
    {{- include "ledger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ledger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
