nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

ingress:
  enabled: true
  annotations: 
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"

service:
  type: ClusterIP
  port: 80

proxy:
  # - name: nas
  #   hosts:
  #     - host: nas.k8s
  #       paths: ["/"]
  #   backend:
  #     ip: 192.168.22.10
  #     port: 5000